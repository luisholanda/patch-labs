syntax = "proto3";

package pl.api;

// Default identifier type used throughout PL APIs.
//
// It is an opaque string that clients should not have to play around
// with.
message Id {
    string id_str = 1;
}
